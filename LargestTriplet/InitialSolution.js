var Node = function(value) {
    // Our lovely Node class.
};

var LinkedList = function() {}; // Feel free to add something here (or not).

LinkedList.prototype.addToTail = function(value) {
    // This method adds a Node to the tail of the linked list.
};

LinkedList.prototype.addToHead = function(value) {
    // This method adds a Node to the head of the linked list.
};

LinkedList.prototype.removeHead = function() {
    // This method removes a Node from the head of the linked list.
    return; // the node we removed from the head of the linked list
};

LinkedList.prototype.removeTail = function() {
    // This method removes a Node from the tail of the linked list.
    return; // the node we removed from the tail of the linked list
};

LinkedList.prototype.search = function(searchValueOrFunction, currentNode) {
    return; // what we are searching for in our linked list
};
